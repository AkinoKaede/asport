# asport-hyphae-handshake

This crate is an in-tree, maintained fork of `hyphae-handshake`, merged into the `asport` workspace because the upstream project is currently unmaintained.

The Rust library name is kept as `hyphae_handshake` for API compatibility.
